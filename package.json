{
  "name": "pickelball-project",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"node backend/server\" \"cd frontend && set BROWSER=none && npm start\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@payos/node": "^1.0.10",
    "jose": "^6.0.11"
  }
}
